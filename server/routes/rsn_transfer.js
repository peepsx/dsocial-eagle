let express = require('express');
let config = require('../config/arisen');
let rsn = require('arisenjsv1');

let router = express.Router();

router.post('/rsn-transfer', (req, res) => {
        let { } = req.body;
})

module.exports = router;